'use strict';

/**
 * @ngdoc function
 * @name nuxeoApp.controller:SandboxCtrl
 * @description
 * # SandboxCtrl
 * Controller of the nuxeoApp
 */
angular.module('nuxeoApp')
  .controller('PersonalworkspaceCtrl', function ($rootScope) {
    $rootScope.navTitle = 'Personal Workspace';
  });
