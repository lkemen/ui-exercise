'use strict';

/**
 * @ngdoc function
 * @name nuxeoApp.controller:FavoritesCtrl
 * @description
 * # FavoritesCtrl
 * Controller of the nuxeoApp
 */
angular.module('nuxeoApp')
  .controller('FavoritesCtrl', function ($rootScope) {
    $rootScope.navTitle = 'Favorites';
  });
